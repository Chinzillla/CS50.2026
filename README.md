# CS50.2026 

## Instructions to run Python lessons:

Activate the virtual environment:
```bash
.venv\Scripts\Activate
```
This works for me using cmd in windows, but you may have to find the command suitable for your CLI

Upgrade pip:
```bash
python.exe -m pip install --upgrade pip
```

Install requirements:
```bash
pip install -r requirements.txt
```